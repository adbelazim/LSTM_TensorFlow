#from __future__ import division
import os
import sys
#import config 
import numpy as np
from scipy import signal
import matplotlib.pyplot as plt






##TO DO, Pruebas
#def pearson_corr(y_true,y_pred):
#	return pearson(y_true,y_pred)

#log_loss = tf.losses.log_loss(y_true,y_pred)

#def pearson_def(y_true, y_pred):
#    assert len(y_true) == len(y)
#    n = len(y_true)
#    assert n > 0
#    avg_x = average(y_true)
#    avg_y = average(y_pred)
#    diffprod = 0
#    xdiff2 = 0
#    ydiff2 = 0
#    for idx in range(n):
#        xdiff = x[idx] - avg_x
#        ydiff = y[idx] - avg_y
#        diffprod += xdiff * ydiff
#        xdiff2 += xdiff * xdiff
#        ydiff2 += ydiff * ydiff

#    return diffprod / math.sqrt(xdiff2 * ydiff2)




